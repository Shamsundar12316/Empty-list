// part 26 // question 31
// no user; add an if test to exercise 30 to make sure the list of users is not empty
// if the list is empty , print the message we need to find some users!
// remove all of the username from your array,and make sure the correct message is printed
var users = [];
if (users.length > 0) {
    console.log("we have enough users");
}
else {
    console.log("we need find some users!");
}
